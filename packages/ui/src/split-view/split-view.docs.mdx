import { ArgTypes, Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';

import * as SplitViewStories from './split-view.stories';

<Meta of={SplitViewStories} />

# Split View

`NSplitView` is the resizable panel layout pattern for app workspaces. It provides horizontal or vertical split panes and serves as the base for queue, preview, timeline, and inspector compositions in the design system.

<div className="n-doc-tabs">
  <input className="n-doc-tabs__input" type="radio" name="split-view-doc-tabs" id="split-view-doc-tab-features" value="features" aria-label="Features" defaultChecked />
  <input className="n-doc-tabs__input" type="radio" name="split-view-doc-tabs" id="split-view-doc-tab-api" value="api" aria-label="API" />
  <input className="n-doc-tabs__input" type="radio" name="split-view-doc-tabs" id="split-view-doc-tab-theming" value="theming" aria-label="Theming" />
  <input className="n-doc-tabs__input" type="radio" name="split-view-doc-tabs" id="split-view-doc-tab-playground" value="playground" aria-label="Playground" />

  <div className="n-doc-tabs__list" aria-label="Split View documentation sections">
    <label className="n-doc-tabs__tab" data-tab="features" htmlFor="split-view-doc-tab-features">Features</label>
    <label className="n-doc-tabs__tab" data-tab="api" htmlFor="split-view-doc-tab-api">API</label>
    <label className="n-doc-tabs__tab" data-tab="theming" htmlFor="split-view-doc-tab-theming">Theming</label>
    <label className="n-doc-tabs__tab" data-tab="playground" htmlFor="split-view-doc-tab-playground">Playground</label>
  </div>

  <section className="n-doc-tabs__panel" data-tab="features" id="features">

## Features

### Import

```ts
import { NSplitView } from '@neural/angular-ui/split-view';
```

### Three-Pane Workspace

The default story follows the app-pattern reference: queue, preview, and inspector panes composed from nested split views.

<Canvas of={SplitViewStories.Default} layout="padded" sourceState="shown" />

  </section>

  <section className="n-doc-tabs__panel" data-tab="api" id="api">

## API

Defines the input properties of `NSplitView`.

<ArgTypes of={SplitViewStories} sort="requiredFirst" />

### Projection Slots

Use `[panelFirst]` and `[panelSecond]` on projected children to assign pane content.

  </section>

  <section className="n-doc-tabs__panel" data-tab="theming" id="theming">

## Theming

Classes include `.n-split-view`, `.n-split-view--horizontal`, `.n-split-view--vertical`, `.n-split-view__panel`, `.n-split-view__gutter`, and `.n-split-view__gutter-handle`.

The split view consumes border, transition, and surface tokens while leaving panel content styling to the composed app surface.

  </section>

  <section className="n-doc-tabs__panel" data-tab="playground" id="playground">

## Playground

Interact with the controls below to tune orientation and split-size constraints.

<Canvas of={SplitViewStories.Playground} layout="padded" sourceState="hidden" />
<Controls of={SplitViewStories.Playground} />

  </section>
</div>
