# HU-011 — Theming Plus: light theme real, density, radius presets y theme switcher avanzado

## Contexto

El repositorio `neural-angular` ya existe.

Ya se completaron y comitearon las HUs anteriores:

* Fase 0: análisis técnico del design system importado.
* Fase 1A: estructura inicial de `@neural/angular-ui` y token foundation.
* HU-001: Theme Provider mínimo y playground visual de tokens.
* HU-002: `NButton` y `NCard`.
* HU-003: `NIcon`, `NBadge`, `NChip` e integración con Lucide.
* HU-004: Forms, Feedback y Empty States.
* HU-005: Layout MVP.
* HU-006: Data Display MVP.
* HU-007: Overlay/Feedback MVP con Angular CDK.
* HU-008: Media/Upload MVP.
* HU-009: AI Interaction MVP.
* HU-010: Motion MVP.

Ahora vamos a reforzar la capa de theming para que el MVP del UI kit sea más profesional, configurable y presentable.

Esta HU implementa:

```txt
Light theme real
Density presets
Radius presets
Contrast mode básico
Theme service mejorado
NThemeToggle
NThemeSwitcher
NAppearancePanel
Playground appearance demo
Documentación de theming avanzado
```

---

## Historia de Usuario

Como desarrollador de Neural Angular,
quiero tener una capa de apariencia configurable con tema, densidad, radios y contraste,
para poder adaptar el UI kit a productos distintos sin modificar componentes uno por uno.

---

## Objetivo principal

Evolucionar el theming de `@neural/angular-ui` desde un provider básico hacia una capa de apariencia MVP lista para producto.

Al terminar esta HU, el usuario debe poder configurar:

* Tema: dark, light, system.
* Densidad: comfortable, compact, spacious.
* Radio visual: sharp, soft, rounded, pill.
* Contraste: normal, high.
* Persistencia opcional en localStorage.
* Switchers visuales en playground.
* Tokens visuales consistentes para dark/light.

---

# 1. Revisar estado actual de theme

Antes de implementar, revisar:

```txt
packages/ui/src/theme/
packages/ui/src/styles/neural-theme.dark.css
packages/ui/src/styles/neural-theme.light.css
packages/ui/src/styles/neural-tokens.css
packages/ui/src/styles/index.css
packages/ui/src/styles/neural-material-map.css
packages/ui/src/styles/neural-compat.css
```

Identificar:

* Cómo se aplica actualmente `data-n-theme`.
* Si existe `NeuralThemeService`.
* Si existe `provideNeuralTheme`.
* Qué tokens light existen.
* Qué tokens dark existen.
* Si ya hay storage.
* Si ya hay system preference.

No romper compatibilidad con HU-001.

---

# 2. Ampliar tipos de theme

Actualizar o crear:

```txt
packages/ui/src/theme/theme.types.ts
```

Tipos esperados:

```ts
export type NeuralThemeName =
  | 'dark'
  | 'light'
  | 'system';

export type NeuralResolvedTheme =
  | 'dark'
  | 'light';

export type NeuralDensity =
  | 'compact'
  | 'comfortable'
  | 'spacious';

export type NeuralRadiusPreset =
  | 'sharp'
  | 'soft'
  | 'rounded'
  | 'pill';

export type NeuralContrast =
  | 'normal'
  | 'high';

export interface NeuralThemeConfig {
  defaultTheme?: NeuralThemeName;
  defaultDensity?: NeuralDensity;
  defaultRadius?: NeuralRadiusPreset;
  defaultContrast?: NeuralContrast;
  storage?: boolean;
  storageKey?: string;
  syncSystemTheme?: boolean;
  transitionThemeChange?: boolean;
}
```

Defaults esperados:

```ts
defaultTheme = 'dark';
defaultDensity = 'comfortable';
defaultRadius = 'rounded';
defaultContrast = 'normal';
storage = false;
storageKey = 'neural-theme';
syncSystemTheme = true;
transitionThemeChange = true;
```

---

# 3. Mejorar provideNeuralTheme

Actualizar:

```txt
packages/ui/src/theme/theme-provider.ts
```

Debe seguir soportando el uso anterior:

```ts
provideNeuralTheme({
  defaultTheme: 'dark',
  storage: false
})
```

Pero ahora también debe soportar:

```ts
provideNeuralTheme({
  defaultTheme: 'system',
  defaultDensity: 'comfortable',
  defaultRadius: 'rounded',
  defaultContrast: 'normal',
  storage: true,
  storageKey: 'neural-ui-appearance',
  syncSystemTheme: true,
  transitionThemeChange: true
})
```

Reglas:

* No acceder a `window` en el provider directamente.
* No acceder a `document` en el provider directamente.
* El provider solo registra config y servicios.
* Mantener SSR safety.
* Mantener backward compatibility.

---

# 4. Mejorar NeuralThemeService

Actualizar o crear:

```txt
packages/ui/src/theme/theme.service.ts
```

Debe exponer una API pública usable.

API sugerida:

```ts
@Injectable({ providedIn: 'root' })
export class NeuralThemeService {
  readonly theme: Signal<NeuralThemeName>;
  readonly resolvedTheme: Signal<NeuralResolvedTheme>;
  readonly density: Signal<NeuralDensity>;
  readonly radius: Signal<NeuralRadiusPreset>;
  readonly contrast: Signal<NeuralContrast>;

  setTheme(theme: NeuralThemeName): void;
  toggleTheme(): void;

  setDensity(density: NeuralDensity): void;
  setRadius(radius: NeuralRadiusPreset): void;
  setContrast(contrast: NeuralContrast): void;

  resetAppearance(): void;
}
```

Si el repo todavía no usa signals para servicios, se permite usar `BehaviorSubject` o el patrón ya existente, pero preferir Angular moderno si encaja.

## Atributos esperados en DOM

El service debe aplicar al elemento raíz del documento:

```html
<html
  data-n-theme="dark"
  data-n-density="comfortable"
  data-n-radius="rounded"
  data-n-contrast="normal"
>
```

O si el repo ya usa `body`, mantener consistencia actual.

Preferencia:

```txt
document.documentElement
```

Pero siempre usando `DOCUMENT` e `isPlatformBrowser`.

## Reglas de SSR

* No usar `window` sin guard.
* No usar `document` global.
* No usar `localStorage` sin guard.
* No usar `matchMedia` sin guard.
* En server, no debe lanzar error.
* En server, debe resolver defaults sin tocar DOM.

## System theme

Si `theme = 'system'`:

* Usar `matchMedia('(prefers-color-scheme: dark)')` solo en browser.
* Resolver a `dark` o `light`.
* Escuchar cambios de sistema solo si `syncSystemTheme = true`.
* Limpiar listener si aplica.

---

# 5. Storage

Si `storage = true`, persistir apariencia.

Estrategia simple:

```ts
{
  theme: 'dark',
  density: 'comfortable',
  radius: 'rounded',
  contrast: 'normal'
}
```

Key default:

```txt
neural-theme
```

Si `storageKey` existe, usarla.

Reglas:

* Leer storage solo en browser.
* Escribir storage solo en browser.
* Si el valor guardado es inválido, ignorarlo y usar defaults.
* No romper si localStorage falla.
* No lanzar errores.

---

# 6. Crear light theme real

Actualizar:

```txt
packages/ui/src/styles/neural-theme.light.css
```

El light theme debe ser usable, no solo inversión rápida.

Debe definir tokens para:

```txt
background/canvas
surface 1-4
surface elevated
borders
text primary
text secondary
text muted
primary
secondary
tertiary
success
warning
danger
info
focus ring
shadow/elevation
overlay
field bg/border
table/header
sidebar/toolbar
chat bubbles
ai surfaces
media surfaces
```

Mantener el naming público `--n-*`.

Ejemplo conceptual:

```css
:root[data-n-theme='light'] {
  --n-color-canvas: #f7f8fb;
  --n-color-background: #ffffff;
  --n-color-surface-1: #ffffff;
  --n-color-surface-2: #f3f5f9;
  --n-color-surface-3: #e9edf5;

  --n-color-text: #111827;
  --n-color-text-secondary: #374151;
  --n-color-text-muted: #6b7280;

  --n-color-border: rgba(15, 23, 42, 0.12);
}
```

No es obligatorio usar exactamente esos valores. Debe armonizar con la identidad Neural/Gemini.

## Reglas

* No romper dark theme.
* No eliminar compat `--nn-*` si existe.
* No crear tokens duplicados innecesarios.
* No hacer hardcode masivo dentro de componentes.
* Usar tokens `--n-*`.

---

# 7. Density presets

Crear o actualizar CSS para density:

```txt
packages/ui/src/styles/neural-density.css
```

O incluir en tokens/theme si el repo ya lo organiza distinto.

Debe soportar:

```css
:root[data-n-density='compact'] {
  --n-control-height-sm: 28px;
  --n-control-height-md: 36px;
  --n-control-height-lg: 44px;
  --n-component-padding-sm: 0.375rem;
  --n-component-padding-md: 0.625rem;
  --n-component-padding-lg: 0.875rem;
}

:root[data-n-density='comfortable'] {
  --n-control-height-sm: 32px;
  --n-control-height-md: 40px;
  --n-control-height-lg: 48px;
}

:root[data-n-density='spacious'] {
  --n-control-height-sm: 36px;
  --n-control-height-md: 44px;
  --n-control-height-lg: 52px;
}
```

Adaptar nombres a los tokens reales existentes.

## Reglas

* No refactorizar todos los componentes manualmente si ya usan tokens.
* Donde haya alturas hardcodeadas en componentes anteriores, migrarlas a tokens de control si es razonable.
* Priorizar:

  * `NButton`
  * `NInput`
  * `NTextarea`
  * `NSelect`
  * `NChip`
  * `NBadge`
  * `NToolbar`
  * `NTable`

No bloquear la HU si algún componente queda con ajuste pendiente; documentarlo.

---

# 8. Radius presets

Crear o actualizar CSS para radius:

```txt
packages/ui/src/styles/neural-radius.css
```

Debe soportar:

```css
:root[data-n-radius='sharp'] {
  --n-radius-xs: 2px;
  --n-radius-sm: 4px;
  --n-radius-md: 6px;
  --n-radius-lg: 8px;
  --n-radius-xl: 12px;
  --n-radius-full: 999px;
}

:root[data-n-radius='soft'] {
  --n-radius-xs: 4px;
  --n-radius-sm: 6px;
  --n-radius-md: 8px;
  --n-radius-lg: 12px;
  --n-radius-xl: 16px;
  --n-radius-full: 999px;
}

:root[data-n-radius='rounded'] {
  --n-radius-xs: 6px;
  --n-radius-sm: 8px;
  --n-radius-md: 12px;
  --n-radius-lg: 16px;
  --n-radius-xl: 24px;
  --n-radius-full: 999px;
}

:root[data-n-radius='pill'] {
  --n-radius-xs: 999px;
  --n-radius-sm: 999px;
  --n-radius-md: 999px;
  --n-radius-lg: 999px;
  --n-radius-xl: 999px;
  --n-radius-full: 999px;
}
```

Reglas:

* Mantener `--n-radius-full`.
* No romper componentes visualmente.
* Documentar que `pill` puede ser extremo y orientado a UIs suaves/futuristas.

---

# 9. Contrast mode básico

Crear o actualizar CSS:

```txt
packages/ui/src/styles/neural-contrast.css
```

Debe soportar:

```css
:root[data-n-contrast='high'] {
  --n-color-border: ...;
  --n-color-text-muted: ...;
  --n-focus-ring-width: 3px;
}
```

Objetivo:

* Mejorar contraste de texto muted.
* Mejorar bordes.
* Mejorar focus ring.
* Mejorar estados danger/warning/info si es necesario.

No intentar cumplir certificación completa en esta HU, pero sí mejorar la accesibilidad visual.

---

# 10. Theme transition

Si `transitionThemeChange = true`, agregar transición ligera al cambio de theme.

Crear clase o atributo temporal si ya existe patrón.

Opción simple:

```css
:root[data-n-theme-transition='true'] *,
:root[data-n-theme-transition='true'] {
  transition:
    background-color 160ms ease,
    border-color 160ms ease,
    color 160ms ease,
    box-shadow 160ms ease;
}
```

Reglas:

* Respetar `prefers-reduced-motion`.
* No animar propiedades pesadas.
* No dejar transición permanente si causa problemas.
* Si es complejo, dejar solo CSS simple.

---

# 11. Crear NThemeToggle

Crear:

```txt
packages/ui/src/theme-toggle/
```

Archivos:

```txt
theme-toggle.component.ts
theme-toggle.component.html
theme-toggle.component.scss
theme-toggle.types.ts
index.ts
```

Selector:

```html
<n-theme-toggle></n-theme-toggle>
```

## API

Inputs:

```ts
showLabel?: boolean;
size?: 'sm' | 'md' | 'lg';
variant?: 'button' | 'switch';
```

Defaults:

```ts
showLabel = false;
size = 'md';
variant = 'button';
```

Uso:

```html
<n-theme-toggle />

<n-theme-toggle
  variant="switch"
  [showLabel]="true"
/>
```

Comportamiento:

* Usa `NeuralThemeService`.
* Alterna entre light y dark.
* Si el theme actual es `system`, alternar hacia el opuesto del resolved actual.
* Usar `NButton` y `NIcon` si existen.
* Iconos sugeridos:

  * dark: `moon`
  * light: `sun`
  * system: `monitor` si está registrado; si no, usar `settings`.

---

# 12. Crear NThemeSwitcher

Crear:

```txt
packages/ui/src/theme-switcher/
```

Selector:

```html
<n-theme-switcher></n-theme-switcher>
```

## Objetivo

Selector visual para cambiar theme, density, radius y contrast de forma compacta.

## API

Inputs:

```ts
showTheme?: boolean;
showDensity?: boolean;
showRadius?: boolean;
showContrast?: boolean;
layout?: 'inline' | 'stacked';
```

Defaults:

```ts
showTheme = true;
showDensity = true;
showRadius = true;
showContrast = true;
layout = 'stacked';
```

Uso:

```html
<n-theme-switcher />

<n-theme-switcher layout="inline" />
```

Implementación:

* Puede usar `NSelect` si ya existe.
* Puede usar `NChip` o `NButton` si queda mejor.
* Mantenerlo simple y funcional.
* No crear overlay ni popover aquí.

Debe permitir seleccionar:

Theme:

```txt
dark
light
system
```

Density:

```txt
compact
comfortable
spacious
```

Radius:

```txt
sharp
soft
rounded
pill
```

Contrast:

```txt
normal
high
```

---

# 13. Crear NAppearancePanel

Crear:

```txt
packages/ui/src/appearance-panel/
```

Selector:

```html
<n-appearance-panel></n-appearance-panel>
```

## Objetivo

Panel más bonito para playground/docs que muestre controles de apariencia y preview.

Uso:

```html
<n-appearance-panel />
```

Debe renderizar:

* Card contenedora.
* Theme switcher.
* Preview mini de componentes:

  * button
  * card
  * badge
  * chip
  * input
  * progress
* Botón reset.

Puede usar componentes existentes.

## API

Inputs:

```ts
title?: string;
description?: string;
showPreview?: boolean;
```

Defaults:

```ts
title = 'Appearance';
description = 'Customize Neural Angular UI appearance.';
showPreview = true;
```

---

# 14. Actualizar style imports

Actualizar:

```txt
packages/ui/src/styles/index.css
```

Debe importar:

```css
@import "./neural-theme.dark.css";
@import "./neural-theme.light.css";
@import "./neural-density.css";
@import "./neural-radius.css";
@import "./neural-contrast.css";
```

Adaptar rutas según estructura real.

No romper imports existentes.

---

# 15. Actualizar exports públicos

Actualizar:

```txt
packages/ui/src/index.ts
packages/ui/package.json
```

Agregar exports:

```txt
./theme-toggle
./theme-switcher
./appearance-panel
```

Revisar que `./theme` siga exportando:

```ts
export * from './theme.types';
export * from './theme-provider';
export * from './theme.service';
```

No romper exports existentes.

---

# 16. Playground — Appearance Demo

Actualizar `apps/playground`.

Agregar sección:

```txt
Theming Plus / Appearance
```

Debe mostrar:

```txt
Theme Toggle
Theme Switcher
Appearance Panel
Light Theme Preview
Density Preview
Radius Preview
Contrast Preview
Component Stress Test
```

## Demo básica

```html
<n-page-header
  eyebrow="Appearance"
  title="Theming Plus"
  description="Switch theme, density, radius and contrast using Neural Angular UI."
  icon="sparkles"
/>

<n-command-bar align="between">
  <div>
    <n-badge variant="success" [dot]="true">Theme Ready</n-badge>
    <n-chip selected>Dark / Light</n-chip>
    <n-chip>Density</n-chip>
    <n-chip>Radius</n-chip>
  </div>

  <n-theme-toggle />
</n-command-bar>

<n-appearance-panel />
```

## Stress test

Crear grid que muestre:

* `NButton`
* `NCard`
* `NInput`
* `NSelect`
* `NBadge`
* `NChip`
* `NProgress`
* `NTable`
* `NPromptInput`
* `NToast` opcional
* `NDialog` opcional

La idea es validar visualmente que light/dark/density/radius/contrast no rompen componentes.

---

# 17. Documentación

Crear:

```txt
docs/ui/theming-plus.md
docs/ui/theme-toggle.md
docs/ui/theme-switcher.md
docs/ui/appearance-panel.md
```

Actualizar:

```txt
docs/ui/getting-started.md
docs/ui/tokens.md
docs/ui/implementation-plan.md
docs/ui/component-inventory.md
packages/ui/README.md
```

## theming-plus.md debe explicar

* `provideNeuralTheme`.
* Config avanzada.
* DOM attributes:

  * `data-n-theme`
  * `data-n-density`
  * `data-n-radius`
  * `data-n-contrast`
* Storage.
* System theme.
* Light theme.
* Density presets.
* Radius presets.
* Contrast mode.
* SSR safety.

## theme-toggle.md

* Uso de `NThemeToggle`.
* Variants.
* Accesibilidad.

## theme-switcher.md

* Uso de `NThemeSwitcher`.
* Inputs.
* Cuándo usarlo.

## appearance-panel.md

* Uso de `NAppearancePanel`.
* Principalmente para playground, docs o settings pages.

---

# 18. Tests

Si ya existe test runner configurado, crear pruebas mínimas.

## NeuralThemeService

* Inicializa defaults.
* Setea theme.
* Toggle theme.
* Setea density.
* Setea radius.
* Setea contrast.
* No rompe en server.
* Ignora valores inválidos de storage si se puede testear.

## NThemeToggle

* Renderiza.
* Llama toggleTheme.
* Muestra icono según theme.

## NThemeSwitcher

* Renderiza opciones.
* Cambia theme.
* Cambia density.
* Cambia radius.
* Cambia contrast.

## NAppearancePanel

* Renderiza título.
* Renderiza switcher.
* Renderiza preview si showPreview = true.

Si no hay test runner configurado, no instalar uno solo para esta HU. Documentar que queda pendiente.

---

# 19. Accesibilidad

* Controles deben ser botones/selects reales.
* Labels visibles o `aria-label`.
* Focus visible.
* Contrast high debe mejorar visibilidad.
* No depender solo de color para comunicar estados.
* Theme transition debe respetar reduced motion.

---

# 20. SSR Safety

No usar directamente:

```txt
window
document
localStorage
sessionStorage
matchMedia
querySelector
getElementById
```

Permitido con guards:

```txt
DOCUMENT
isPlatformBrowser
matchMedia
localStorage
```

Reglas:

* En SSR no tocar DOM.
* En SSR no leer localStorage.
* En SSR no usar matchMedia.
* En SSR usar defaults.
* No lanzar errores.

---

# 21. Fuera de alcance

No hacer todavía:

```txt
Theme builder visual completo
Exportar temas a JSON
Importar temas externos
Editor avanzado de tokens
Per-component theme overrides
Brand palettes dinámicas
Design token pipeline con Style Dictionary
Figma plugin
CLI theme generator
Material theme profundo
Storybook
SSR package
Schematics
Adapters
```

---

# 22. Criterios de aceptación

La HU estará completa cuando:

1. `provideNeuralTheme` soporte config avanzada.
2. `NeuralThemeService` gestione theme, resolvedTheme, density, radius y contrast.
3. Se apliquen atributos `data-n-theme`, `data-n-density`, `data-n-radius`, `data-n-contrast`.
4. Exista light theme real usable.
5. Existan density presets.
6. Existan radius presets.
7. Exista contrast mode básico.
8. Exista `NThemeToggle`.
9. Exista `NThemeSwitcher`.
10. Exista `NAppearancePanel`.
11. Todos sean standalone Angular components.
12. Todos usen tokens `--n-*`.
13. Todo sea SSR-safe.
14. Exports públicos estén actualizados.
15. Playground tenga Theming Plus / Appearance demo.
16. Docs estén creadas/actualizadas.
17. Build/check disponible pase o se documenten limitaciones.

---

# 23. Validación manual

Debe poder ejecutarse:

```bash
pnpm nx serve playground
```

o el comando real del workspace.

En la UI debe verse:

```txt
Neural Angular Playground
Theming Plus / Appearance
Theme Toggle
Theme Switcher
Appearance Panel
Light Theme Preview
Density Preview
Radius Preview
Contrast Preview
Component Stress Test
```

Validar manualmente:

* Cambiar dark/light.
* Cambiar system si el navegador lo permite.
* Cambiar compact/comfortable/spacious.
* Cambiar sharp/soft/rounded/pill.
* Cambiar normal/high contrast.
* Refrescar si storage está activo y verificar persistencia.
* Confirmar que los componentes principales siguen viéndose bien.

---

# 24. Resultado esperado

Mostrar resumen con:

```txt
Files created
Files updated
Theme API updated
Tokens/styles added
Components created
Public API added
Playground appearance demo added
Docs added
Tests added or skipped with reason
How to run
Known limitations
Recommended next HU
```

La siguiente HU recomendada debe ser:

```txt
HU-012 — Crear Quality & Release MVP: build library, package exports audit, docs index y release checklist
```

---

## Commit sugerido

```bash
git add .
git commit -m "feat(ui): add advanced theming and appearance controls"
```
